# Loader Configuration
# Map loaders (parsers) and chunkers by file extension.

# Default loader/chunker when no extension mapping is provided
# 💡 Tip: Comment out default_loader to ONLY process registered extensions
#         (automatically ignores .DS_Store, .git, etc.)
# default_loader: txt
default_chunker: paragraph

# Extension-specific mappings (declare special cases only)
extensions:
  .txt:
    loader: txt
    chunker: paragraph

  .md:
    loader: markdown
    chunker: paragraph

  .pdf:
    loader: pdf
    chunker: paragraph

  .docx:
    loader: docx
    chunker: paragraph

  .html:
    loader: html
    chunker: paragraph

  .htm:
    loader: html
    chunker: paragraph

  .py:
    loader: txt
    chunker: paragraph

  .js:
    loader: txt
    chunker: paragraph

  .ts:
    loader: txt
    chunker: paragraph
